from django.urls import path
from . import views

urlpatterns = [
    # Estas rutas se llenarán en las siguientes ramas (vistas y templates)
    # Ejemplo: path('', views.listar_calificaciones, name='listar'),
]